class: CommentLike
table: comment_like

fields:
  commentId: int
  userId: String
  createdAt: DateTime, default=now

indexes:
  comment_like_unique_idx:
    fields: commentId, userId
    unique: true
