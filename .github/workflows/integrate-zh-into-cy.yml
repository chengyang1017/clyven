name: Resolve cy zh all-features merge

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Integration is being resolved directly through the GitHub API."
