import 'package:flutter_application_3/main.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('VideoApp can be constructed', () {
    const app = VideoApp();

    expect(app, isA<VideoApp>());
  });
}
