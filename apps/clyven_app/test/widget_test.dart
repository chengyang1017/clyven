import 'package:clyven_app/main.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('ClyvenApp can be constructed', () {
    const app = ClyvenApp();

    expect(app, isA<ClyvenApp>());
  });
}
