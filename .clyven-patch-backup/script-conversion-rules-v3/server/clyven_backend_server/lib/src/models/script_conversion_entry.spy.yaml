class: ScriptConversionEntry
table: script_conversion_entry
fields:
  profileId: int
  sourceText: String
  targetText: String
  priority: int, default=0
  note: String?
  metadataJson: String?
  createdAt: DateTime, default=now
  updatedAt: DateTime, default=now